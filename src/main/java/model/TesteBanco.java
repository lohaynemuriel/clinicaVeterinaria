package model;

import java.text.ParseException;
import java.text.SimpleDateFormat;  
import java.util.Calendar;
import java.util.List;

/**
 *
 * @author v177950
 */
public class TesteBanco {

    public static void main(String[] args) {
        /*AnimalDAO.getInstance().create("ornitorrinco", 2024, "M", 1, 1);
        System.out.println(AnimalDAO.getInstance().retrieveAll().get(0));
        List<Animal> animais = AnimalDAO.getInstance().retrieveAll();
        System.out.println(animais.get(0));
        for(Animal a : animais){
            System.out.println(a);
        }
        ClienteDAO.getInstance().create("x", "rua x", "123", "qwer@aasda", "12345678911");
        Cliente lc = ClienteDAO.getInstance().retrieveById(2);
        System.out.println(lc);
        Calendar hj = Calendar.getInstance(); 
        hj.set(2020, Calendar.OCTOBER, 20);
        ConsultaDAO.getInstance().create(hj, "6 e onibus", "bela consulta", 1, 1, 1);
        List<Consulta> cs = ConsultaDAO.getInstance().retrieveAll();
        System.out.println(cs.get(0));
        for(Animal a : animais){
            System.out.println(a);
        }
        EspecieDAO.getInstance().create("aquatico");
        List<Especie> es = EspecieDAO.getInstance().retrieveAll();
        System.out.println(es.get(0));
        ExameDAO.getInstance().create("raio X", 1);
        List<Exame> ex = ExameDAO.getInstance().retrieveAll();
        System.out.println(ex.get(0));
        VeterinarioDAO.getInstance().create("Victor", "v177950@dac.unicamp.br", "13988668949");
        List<Veterinario> v = VeterinarioDAO.getInstance().retrieveAll();
        System.out.println(v.get(0));
        System.out.println("oaps");*/
        
        /*EspecieDAO esp = EspecieDAO.getInstance();
        esp.deleteAll();
        List<Especie> especie = esp.retrieveAll();
        for(Especie es : especie){
            System.out.println(es);
        }*/
        
        /*AnimalDAO esp = AnimalDAO.getInstance();
        esp.deleteAll();
        List<Especie> especie = esp.retrieveAll();
        for(Especie es : especie){
            System.out.println(es);
        }*/
    }
}
